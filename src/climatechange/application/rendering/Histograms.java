package climatechange.application.rendering;

import javafx.scene.paint.Color;
import javafx.scene.shape.Line;

public abstract class Histograms {
    public static Line makeHistogram(int latitude, int longitude, Color color, float temperature) {
        //TODO renvoie ligne selon la température. Et dans controller on appelle soit ça soit on fait un quadrilatère
        return null;
    }
}
